import { VehicleServiceDirective } from './vehicle-service.directive';

describe('VehicleServiceDirective', () => {
  it('should create an instance', () => {
    const directive = new VehicleServiceDirective();
    expect(directive).toBeTruthy();
  });
});
